#include <iostream>

namespace VeryEasy
{
    bool returnTrue();
    int flipIntBool(int);
    int circuitPower(int, int);
    int addition(int, int);
    int animals(int, int, int);
    int points(int, int);
}